<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.natrollus.kisisel" >

    <uses-permission android:name="android.permission.BODY_SENSORS"/>
    <uses-permission android:name="com.samsung.permission.SSENSOR_HRM_RAW_PPG"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ikon"
        android:label="@string/app_name">
        <activity android:name=".Bilgilendirme" android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
		<receiver android:name=".Kisisel">
			<intent-filter>
				<action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
				<action android:name="kisisel.action.DUGME"/>
				<action android:name="kisisel.action.TEST"/>
			</intent-filter>
			<meta-data android:name="android.appwidget.provider"
				android:resource="@xml/ayar"/>
		</receiver>
    </application>
</manifest>